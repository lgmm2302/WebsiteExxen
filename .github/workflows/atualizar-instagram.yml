{name: Atualizar instagram, on: workflow_dispatch, permissions: {contents: write}, jobs: {atualizar: {runs-on: ubuntu-latest, steps: [{uses: actions/checkout@v4}, {run: bash atualizar-instagram.sh}]}}}
